# Отчёт об аудите проекта MiniDeN

Дата проверки: 2024

## 🔴 Критические проблемы безопасности

### 1. Жёстко заданный пароль БД по умолчанию
**Файл:** `database.py:19`
```python
DB_PASSWORD = os.getenv("DB_PASSWORD", "strongpassword")
```
**Проблема:** Если переменная окружения не задана, используется слабый пароль по умолчанию.
**Рекомендация:** Убрать значение по умолчанию и выбрасывать исключение, если пароль не задан:
```python
DB_PASSWORD = os.getenv("DB_PASSWORD")
if not DB_PASSWORD:
    raise ValueError("DB_PASSWORD must be set in environment variables")
```

### 2. Дефолтный админ с паролем "admin"
**Файл:** `initdb.py:752-759`
```python
session.add(
    AdminUser(
        username="admin",
        password_hash=hash_password("admin"),
        ...
    )
)
```
**Проблема:** При первом запуске создаётся админ с предсказуемыми учётными данными.
**Рекомендация:** 
- Генерировать случайный пароль и выводить его в логи (только при первом запуске)
- Или требовать явного создания админа через скрипт `create_superadmin.py`
- Добавить предупреждение в документацию

### 3. CORS разрешает все источники
**Файл:** `webapi.py:47`
```python
allow_origins=["*"],
```
**Проблема:** Любой сайт может делать запросы к API.
**Рекомендация:** Ограничить разрешённые домены:
```python
allowed_origins = os.getenv("CORS_ALLOWED_ORIGINS", "").split(",")
allowed_origins = [origin.strip() for origin in allowed_origins if origin.strip()]
if not allowed_origins:
    allowed_origins = ["http://localhost:3000"]  # только для разработки

app.add_middleware(
    CORSMiddleware,
    allow_origins=allowed_origins,
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["*"],
)
```

## ⚠️ Важные проблемы

### 4. Отсутствует `.env.example`
**Проблема:** Нет примера файла с переменными окружения, что усложняет настройку.
**Рекомендация:** Создать `.env.example` с описанием всех необходимых переменных:
```env
# Обязательные
BOT_TOKEN=your_telegram_bot_token
ADMIN_CHAT_ID=123456789
# или
ADMIN_CHAT_IDS=123456789,987654321

# База данных
DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/miniden
# или отдельно:
DB_NAME=miniden
DB_USER=miniden_user
DB_PASSWORD=strong_password_here
DB_HOST=localhost
DB_PORT=5432

# Опциональные
PAYMENTS_PROVIDER_TOKEN=
REQUIRED_CHANNEL_ID=@your_channel
REQUIRED_CHANNEL_LINK=https://t.me/your_channel
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
```

### 5. Нет версионирования зависимостей
**Файл:** `requirements.txt`
**Проблема:** Используются версии без точного указания (например, `aiogram>=3`), что может привести к несовместимости при обновлениях.
**Рекомендация:** Закрепить версии для продакшена:
```txt
aiogram==3.7.0
fastapi==0.104.1
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
...
```

### 6. Отсутствует пул соединений для БД
**Файл:** `database.py:28`
**Проблема:** Не настроен пул соединений, что может привести к проблемам при высокой нагрузке.
**Рекомендация:** Добавить параметры пула:
```python
engine = create_engine(
    DATABASE_URL,
    future=True,
    echo=os.getenv("SQLALCHEMY_ECHO") == "1",
    pool_size=10,
    max_overflow=20,
    pool_pre_ping=True,  # проверка соединения перед использованием
    pool_recycle=3600,   # пересоздание соединений каждый час
)
```

### 7. Debug-эндпоинты доступны в продакшене
**Файлы:** 
- `routes_adminsite.py:533-548`
- `admin_panel/adminsite/router.py:163-174`
**Проблема:** Debug-эндпоинты могут раскрывать внутреннюю информацию.
**Рекомендация:** Отключать их в продакшене:
```python
if os.getenv("DEBUG") != "1":
    # не регистрировать debug роуты
    pass
```

## 📝 Улучшения кода

### 8. TODO комментарий
**Файл:** `services/products.py:126`
```python
# TODO: синхронизировать image_url с основным фото.
```
**Рекомендация:** Либо реализовать, либо создать issue в трекере задач.

### 9. Обработка исключений в `get_db()`
**Файл:** `database.py:52-57`
**Проблема:** Функция `get_db()` не закрывает сессию при исключении.
**Рекомендация:** Улучшить обработку:
```python
def get_db() -> Iterator[Session]:
    db = SessionLocal()
    try:
        yield db
    except Exception:
        db.rollback()
        raise
    finally:
        db.close()
```

### 10. Логирование паролей (потенциальная проблема)
**Рекомендация:** Убедиться, что в логах не попадают чувствительные данные (токены, пароли). Проверить все места логирования.

### 11. Отсутствие rate limiting
**Проблема:** API не защищён от злоупотреблений (DDoS, brute force).
**Рекомендация:** Добавить rate limiting для API-эндпоинтов:
```python
from slowapi import Limiter, _rate_limit_exceeded_handler
from slowapi.util import get_remote_address
from slowapi.errors import RateLimitExceeded

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter
app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)

@router.post("/api/auth/login")
@limiter.limit("5/minute")
def login(...):
    ...
```

### 12. Отсутствие валидации входных данных в некоторых местах
**Рекомендация:** Убедиться, что все пользовательские данные валидируются через Pydantic схемы перед использованием.

## ✅ Положительные моменты

1. ✅ Использование SQLAlchemy ORM (защита от SQL-инъекций)
2. ✅ Хеширование паролей через bcrypt
3. ✅ Использование переменных окружения для конфигурации
4. ✅ Правильная обработка исключений в FastAPI
5. ✅ Использование типизации (type hints)
6. ✅ Структурированное логирование
7. ✅ Разделение на модули и сервисы

## 📋 Рекомендации по приоритетам

### Высокий приоритет (сделать немедленно):
1. Исправить дефолтный пароль БД
2. Убрать или защитить дефолтного админа
3. Ограничить CORS
4. Создать `.env.example`

### Средний приоритет (в ближайшее время):
5. Закрепить версии зависимостей
6. Настроить пул соединений БД
7. Отключить debug-эндпоинты в продакшене
8. Добавить rate limiting

### Низкий приоритет (улучшения):
9. Реализовать TODO
10. Улучшить обработку исключений
11. Добавить больше тестов
12. Настроить мониторинг и алерты

## 🔍 Дополнительные проверки

Рекомендуется также проверить:
- [x] Наличие `.gitignore` для исключения `.env` и других чувствительных файлов ✅ (файл существует и правильно настроен)
- [ ] Настройки безопасности в nginx (если используется)
- [ ] SSL/TLS сертификаты для продакшена
- [ ] Резервное копирование базы данных
- [ ] Логирование и мониторинг ошибок
- [ ] Документация API (Swagger/OpenAPI)

## 📄 Примечания

- `.gitignore` присутствует и правильно исключает `.env` файлы ✅
- В `.gitignore` есть опечатка: `.env.exemple` вместо `.env.example` (не критично, но можно исправить)

---

**Итог:** Проект в целом хорошо структурирован, но есть несколько критических проблем безопасности, которые нужно исправить перед развёртыванием в продакшен.
