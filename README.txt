MiniDeN — Telegram-бот-магазин
===============================

Текущее состояние
-----------------

- Основная база данных: PostgreSQL (через SQLAlchemy из `database.py`).
- Каталог корзинок и курсов, корзина, заказы, избранное и пользователи хранятся в таблицах PostgreSQL.
- Бот и WebApp используют одни и те же данные, JSON-файлы остаются только для первичного наполнения каталога.

Наследие предыдущей версии
--------------------------

Исторически бот поднимался без внешней БД: каталог подгружался из `data/products_baskets.json` и
`data/products_courses.json`, а корзина собиралась в памяти. Эти файлы сохранены для совместимости и
могут использоваться для тестового наполнения, но основное хранилище теперь — PostgreSQL.

---

## История изменений

**2025-11-27**

- Подключена база данных PostgreSQL (`miniden` на порту 5432 через SQLAlchemy).
- Каталог корзинок и курсов, корзина, заказы, избранное и пользователи
  хранятся в таблицах PostgreSQL (`products_baskets`, `products_courses`,
  `cart_items`, `orders`, `favorites`, `users` и др.).
- Исправлена ошибка в админке: кнопка «📋 Товары: корзинки/курсы» теперь
  использует `services.products.list_products_admin(...)` и не падает
  с `TypeError: list_products_by_status() got an unexpected keyword argument 'product_type'`.
- Бот и WebApp используют общую базу данных, пока JSON-файлы служат
  только для начального наполнения каталога (опционально).
