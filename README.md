Проект: Telegram-бот-магазин MiniDeN
====================================

Функционал
----------

1. Каталог товаров
   - Корзинки (тип `basket`)
   - Онлайн-курсы (тип `course`)
   - Пагинация для пользователей:
     - кнопка "🧺 Корзинки"
     - кнопка "🎓 Онлайн-курсы"
   - Карточки товара:
     - название
     - цена
     - описание
     - фото (если есть)
     - ссылка "Подробнее" (если задана)
     - кнопка "Добавить в корзину"

2. Корзина
   - просмотр корзины ("🛒 Корзина")
   - увеличение/уменьшение количества
   - удаление позиции
   - очистка корзины
   - оформление заказа из корзины

3. Оформление заказа
   - сбор данных:
     - имя клиента
     - контакт (телефон / ник)
     - комментарий
   - сохранение заказа в БД:
     - таблица `orders`
     - таблица `order_items`
   - текст заявки для администратора (формируется в `utils/texts.py`)

4. Админ-панель
   - вход по кнопке "⚙️ Админка" (доступ есть только у ID из `ADMIN_IDS` в `config.py`)
   - разделы:
     - "📋 Товары: корзинки"
     - "📋 Товары: курсы"
     - "/orders" — список последних заказов
   - работа с товарами:
     - просмотр списка с фильтром по статусу:
       - все
       - активные
       - скрытые / "удалённые"
     - создание товара (корзинки или курса)
     - редактирование:
       - название
       - цена
       - описание
       - ссылка
       - фото
     - скрытие товара (`is_active = 0`)
     - переключение статуса показа (on/off)

5. Проверка подписки на канал (gating)
   - при `/start` бот проверяет, подписан ли пользователь на указанный канал;
   - если НЕ подписан:
     - показывает экран с текстом:
       - просьба подписаться;
       - кнопка «📎 Перейти в канал»;
       - кнопка «✅ Я подписался»;
     - пока подписки нет, главное меню недоступно;
   - если подписан — сразу открывается стандартное главное меню.

6. Избранное (wishlist)
   - пользователи могут добавлять корзинки и курсы в избранное прямо из каталога;
   - раздел "❤️ Избранное" доступен из главного меню;
   - избранное привязано только к пользователю: администраторы не управляют им отдельно.

7. Промокоды и скидки
   - пользователи могут ввести промокод на этапе оформления заказа;
   - промокод уменьшает итоговую сумму заказа и сохраняется вместе с заявкой;
   - учёт использований ведётся автоматически.

Каталог карточками
------------------

- Корзинки и курсы показываются отдельными карточками: название, цена или «Бесплатно»,
  краткое описание и фото, если загружено в Telegram.
- Под карточками расположены кнопки:
  - «➕ В корзину» — добавить товар/курс в корзину;
  - «🔗 Перейти» — доступна, если есть ссылка и у пользователя открыт доступ к курсу;
  - «❤️ В избранное» / «💔 Убрать из избранного» — переключение избранного.
- При большом количестве товаров включена пагинация: кнопки «◀️ / ▶️» перелистывают
  страницы каталога без спама десятками сообщений.

Стек и структура проекта
------------------------

- Python
- aiogram 3.x
- SQLite (через модуль `database.py`)

Основные директории и файлы:

- `bot.py` — точка входа бота
- `config.py` — настройки бота (токен, админ-IDs, обязательный канал и т.п.)
- `database.py` — создание таблиц и подключение к БД
- `data/`
  - `bot.db` — база данных SQLite
  - `products_baskets.json` — стартовый список корзинок
  - `products_courses.json` — стартовый список курсов
- `handlers/`
  - `start.py` — обработчик `/start`, проверка подписки и показ главного меню
  - `baskets.py` — каталог корзинок с пагинацией
  - `courses.py` — каталог курсов с пагинацией
  - `cart.py` — логика корзины
  - `checkout.py` — оформление заказа
  - `payments.py` — заглушка под будущие платежи
  - `admin.py` — админ-панель, управление товарами и заказами
- `services/`
  - `products.py` — работа с товарами (корзинки + курсы) через БД
    - однократная инициализация таблицы `products` из JSON-файлов
    - получение списков товаров
    - фильтры по статусу
    - создание/редактирование/скрытие товара
  - `cart.py` — операции с корзиной в БД
  - `orders.py` — создание и получение заказов
  - `payments.py` — задел под будущие платежи
- `keyboards/`
  - `main_menu.py` — главное меню
  - `catalog_keyboards.py` — кнопки каталога для пользователей
  - `cart_keyboards.py` — кнопки корзины (увеличить/уменьшить/удалить/оформить)
  - `admin_inline.py` — inline-кнопки админки (списки и действия с товарами)
- `utils/`
  - `logging_config.py` — настройка логирования
  - `texts.py` — форматирование текста для корзины и заказов

  WebApp-страницы
  ---------------

  Папка `webapp/` содержит лендинг и разделы для сайта MiniDeN в едином тёмном стиле:

  - `index.html` — главная страница-лендинг с героем, категориями и CTA-кнопками.
  - `products.html` — витрина товаров (тип `basket`) с категориями и добавлением в корзину через API.
  - `masterclasses.html` — раздел мастер-классов (категории `free`/`paid`) с загрузкой карточек из API и добавлением в корзину.
  - `cart.html` — корзина, связанная с таблицей `cart_items` бота; управление количеством и очистка через API.
  - `profile.html` — макет профиля и блоков «Мои заказы» / «Мои мастер-классы».

  Общий верхний навбар (Главная, Товары, Мастер-классы, Корзина, Профиль) добавлен на все страницы.

  WebApp-кнопки Telegram-бота
  ---------------------------

  Если в окружении заданы ссылки на WebApp-страницы, в главном меню бота появятся соответствующие кнопки (кнопка показывается только при наличии URL):

  - `WEBAPP_INDEX_URL` — URL для кнопки «🏠 Главная (WebApp)».
  - `WEBAPP_PRODUCTS_URL` — URL для кнопки «🛍 Товары (WebApp)».
  - `WEBAPP_MASTERCLASSES_URL` — URL для кнопки «🎓 Мастер-классы (WebApp)».
  - `WEBAPP_CART_URL` — URL для кнопки «🛒 Корзина (WebApp)».
  - `WEBAPP_PROFILE_URL` — URL для кнопки «👤 Профиль (WebApp)».

  Значения этих переменных разработчик прописывает в `.env` вручную; если URL не указан, соответствующая WebApp-кнопка не отображается.

Web API
-------

Файл `webapi.py` поднимает HTTP API (FastAPI) над существующими сервисами и той же SQLite-БД, что использует бот.

Endpoint'ы:

- `GET /api/categories?type=basket|course` — список активных категорий (id, slug, name, sort_order).
- `GET /api/products?type=basket|course&category_slug=...` — активные товары/курсы с привязкой к категории (slug и name возвращаются).
- `GET /api/cart?user_id=123` — корзина пользователя: items (product_id, name, price, qty, category) + итоговая сумма.
- `POST /api/cart/add` — {user_id, product_id, qty?} → добавить позицию в корзину.
- `POST /api/cart/update` — {user_id, product_id, qty} → выставить количество (0/отрицательное удаляет позицию).
- `POST /api/cart/clear` — {user_id} → очистить корзину.

На WebApp-страницах JS-константа `API_BASE` указана как пример (`https://YOUR-API-DOMAIN/api`) — замените домен на реальный.

Промокоды и скидки
------------------

- В админ-панели появилась кнопка "🎟 Промокоды" с возможностью создать новый код или посмотреть список.
- Поддерживаются два типа скидок: процент от суммы заказа или фиксированная сумма.
- Пользователь может ввести промокод при оформлении заказа: бот проверит сроки действия, лимиты и минимальную сумму.
- Применённый промокод сохраняется в заказе вместе с суммой скидки, а количество использований увеличивается автоматически.
- Краткий отчёт по промокодам доступен через команду `/promo_stats` для администраторов.

Работа с БД
-----------

Файл БД: `data/bot.db`

Таблицы:

1. `products`
   - id INTEGER PRIMARY KEY
   - type TEXT NOT NULL       -- 'basket' или 'course'
   - name TEXT NOT NULL
   - price INTEGER NOT NULL
   - description TEXT
   - detail_url TEXT
   - is_active INTEGER NOT NULL DEFAULT 1
   - image_file_id TEXT       -- file_id фото товара в Telegram
   - category_id INTEGER      -- привязка к таблице категорий

   При первом запуске, если таблица `products` пуста:
   - из `data/products_baskets.json` и `data/products_courses.json`
     автоматически импортируются стартовые товары.

2. `categories`
   - id INTEGER PRIMARY KEY AUTOINCREMENT
   - type TEXT NOT NULL       -- 'basket' или 'course'
   - slug TEXT NOT NULL
   - name TEXT NOT NULL
   - sort_order INTEGER DEFAULT 0
   - is_active INTEGER NOT NULL DEFAULT 1

   Стартовые категории:
   - Товары: Корзинки (`baskets`), Люльки (`cradles`), Сумки (`bags`), Другое (`other`).
   - Мастер-классы: Бесплатные (`free`), Платные (`paid`).
   Для существующих записей при миграции категория назначается автоматически по типу и цене.
   В следующих версиях админка WebApp сможет управлять категориями и связью товаров с ними через API.

3. `cart_items`
   - user_id INTEGER NOT NULL
   - product_id TEXT NOT NULL
   - name TEXT
   - price INTEGER
   - qty INTEGER
   - PRIMARY KEY (user_id, product_id)

4. `orders`
   - id INTEGER PRIMARY KEY AUTOINCREMENT
   - user_id INTEGER
   - user_name TEXT
   - customer_name TEXT
   - contact TEXT
   - comment TEXT
   - total INTEGER
   - promocode_code TEXT
   - discount_amount INTEGER
   - status TEXT
   - order_text TEXT
   - created_at TEXT

5. `order_items`
   - id INTEGER PRIMARY KEY AUTOINCREMENT
   - order_id INTEGER NOT NULL
   - product_name TEXT
   - price INTEGER
   - qty INTEGER
   - FOREIGN KEY (order_id) REFERENCES orders (id) ON DELETE CASCADE

6. `promocodes`
   - id INTEGER PRIMARY KEY AUTOINCREMENT
   - code TEXT NOT NULL UNIQUE
   - discount_type TEXT NOT NULL ('percent' или 'fixed')
   - discount_value INTEGER NOT NULL
   - min_order_total INTEGER DEFAULT 0
   - max_uses INTEGER DEFAULT 0
   - used_count INTEGER NOT NULL DEFAULT 0
   - is_active INTEGER NOT NULL DEFAULT 1
   - valid_from TEXT
   - valid_to TEXT
   - description TEXT

Настройки через .env
--------------------

В `.env` должны быть заданы:

Обязательные:

- `BOT_TOKEN` — токен вашего Telegram-бота.
- `ADMIN_CHAT_ID` или `ADMIN_CHAT_IDS` — ID администратора(ов).

Опциональные:

- `PAYMENTS_PROVIDER_TOKEN` — токен платёжного провайдера (на будущее).
- `REQUIRED_CHANNEL_ID` — канал, на который нужно быть подписанным:
  - пример: `@miniden_channel`
  - пример: `-1001234567890`
- `REQUIRED_CHANNEL_LINK` — ссылка на канал:
  - пример: `https://t.me/miniden_channel`
  - если не указана, а `REQUIRED_CHANNEL_ID` начинается с `@`,
    ссылка будет собрана автоматически как `https://t.me/<username>`.

Как запустить
-------------

1. Установить зависимости (пример):

   - создать виртуальное окружение
   - установить aiogram 3.x и другие пакеты из `requirements.txt` (если есть)

2. Настроить `.env`:
   - `BOT_TOKEN=...`
   - `ADMIN_CHAT_ID=...` или `ADMIN_CHAT_IDS=...`
   - `REQUIRED_CHANNEL_ID=@имя_канала` (если нужна проверка подписки)
   - `REQUIRED_CHANNEL_LINK=https://t.me/имя_канала` (по желанию)

3. Убедиться, что в папке `data/` есть файлы:
   - `products_baskets.json`
   - `products_courses.json`

4. Запустить:

   python bot.py

При первом запуске:
- создаётся база `data/bot.db`
- создаются таблицы
- если таблица `products` пустая — товары подгружаются из JSON.

Примечания
----------

- "Удаление" товара сейчас реализовано как `soft delete`:
  - товар помечается `is_active = 0`
  - его можно вернуть через переключение статуса.
- Пользовательский каталог показывает только активные товары (`is_active = 1`).
- Проверка подписки включается только если задан `REQUIRED_CHANNEL_ID`.
- В будущем можно добавить:
  - физическое удаление товара
  - восстановление из "корзины"
  - пагинацию в админке по списку товаров и заказов.
Проект: Telegram-бот-магазин MiniDeN
====================================

Нижняя клавиатура
-----------------

- До прохождения проверки подписки:
  - показывается только одна кнопка «🔵 Старт»;
  - при нажатии на неё бот проверяет подписку на канал и:
    - если пользователь подписан — открывает главное меню;
    - если не подписан — пишет, что нужно подписаться, и даёт кнопки:
      «📎 Перейти в канал» и «✅ Я подписался».
- После успешной проверки:
  - показывается полное главное меню:
    - «🔵 Старт» (можно нажать заново, чтобы перепроверить подписку),
    - «🧺 Корзинки»,
    - «🎓 Онлайн-курсы»,
    - «🛒 Корзина»,
    - «⚙️ Админка» (только для админов).

Остальное описание проекта (каталог, корзина, заказы, админка, структура файлов)
остаётся таким же, как мы уже фиксировали ранее.
